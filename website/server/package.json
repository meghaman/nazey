{
  "name": "expresstestbed",
  "version": "1.0.0",
  "description": "",
  "main": "server/index.js",
  "scripts": {
    "client": "cd ../client && npm start",
    "server": "nodemon server.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build-client" : "cd ../client && npm run build",
    "build" : "npm run build-client && ./build.sh"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^1.18.9"
  },
  "dependencies": {
    "express": "^4.16.3",
    "uuid": "^3.3.2"
  }
}
